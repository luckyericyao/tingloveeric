# Audio

`our-night.m4a` is an original ambient loop generated for this project by
`scripts/generate_ambient.py`. It can be replaced by changing `music.src` in
`src/data/storyWorld.ts`.

The player starts only after the visitor presses the entry button, stays muted
when requested, and exposes volume controls in the story interface.
